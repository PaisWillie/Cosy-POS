import 'package:flutter/material.dart';

class OrderQueueItem extends StatelessWidget {
  const OrderQueueItem({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
