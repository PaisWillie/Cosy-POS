import 'package:flutter/material.dart';

class MenuCheckoutItem extends StatelessWidget {
  const MenuCheckoutItem({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
